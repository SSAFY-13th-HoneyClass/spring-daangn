spring:
  datasource:
    url: jdbc:mysql://localhost:3307/daangn?useSSL=false&serverTimezone=Asia/Seoul
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    open-in-view: false

  # ?? ?? ?? ?? (?? ?)
  mvc:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  web:
    locale: ko
