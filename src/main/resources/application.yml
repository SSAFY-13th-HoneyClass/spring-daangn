spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-1.cr6o8qs8ufwt.ap-northeast-2.rds.amazonaws.com/Daangn?createDatabaseIfNotExist=true
    username: admin
    password:

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    show_sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
#        database-platform: org.hibernate.dialect.MySQL8Dialect
#
#        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret: