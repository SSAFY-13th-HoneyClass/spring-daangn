server:
  port: ${SERVER_PORT}

spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-1.c3k88oim28pu.ap-northeast-2.rds.amazonaws.com:3306/Daangn?createDatabaseIfNotExist=true
    username: admin
    password: taeho7882

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show_sql: ${JPA_SHOW_SQL}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        database-platform: org.hibernate.dialect.MySQL8Dialect

        format_sql: false


logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret: ${JWT_TOKEN}
